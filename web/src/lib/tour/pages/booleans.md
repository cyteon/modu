## Booleans
Booleans can be defined with true/false, or converted from other values with `bool(value)`, and can also be converted to an int with `int(bool)` (1 = true, 0 = false).

[CODE]
let a = true;

print(!a); // false
print(!false); // true